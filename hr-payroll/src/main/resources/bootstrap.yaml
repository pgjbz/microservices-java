server:
  port: ${PORT:0}

spring:
  application:
    name: payroll
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      fail-fast: true

services:
  hr-worker:
    url: localhost:8001
    path: /workers
    name: worker